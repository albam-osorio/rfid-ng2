import { Routes } from '@angular/router';

import { PrimariaComponent }    from './primaria.component';

export const PrimariaRoutes: Routes = [
    {
        path: 'tms-p',
        component: PrimariaComponent
    }
];
