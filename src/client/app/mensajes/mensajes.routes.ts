import { Routes } from '@angular/router';

import { MensajesComponent }    from './mensajes.component';

export const MensajesRoutes: Routes = [
    {
        path: 'mensajes',
        component: MensajesComponent
    }
];
