import { Routes } from '@angular/router';

import { SecundariaComponent }    from './secundaria.component';

export const SecundariaRoutes: Routes = [
    {
        path: 'tms-s',
        component: SecundariaComponent
    }
];
